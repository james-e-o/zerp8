'use client'

import UserSidebarLayout from '@/components/user-sidebar-layout'

export default function CompanyInvitesLayout({ children }) {
  return <UserSidebarLayout>{children}</UserSidebarLayout>
}
