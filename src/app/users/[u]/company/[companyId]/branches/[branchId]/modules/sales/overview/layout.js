// Overview is a single dashboard view — no sub-navigation needed,
// so this layout is a pass-through kept only for structural symmetry
// with the other Sales routes.
export default function OverviewLayout({ children }) {
  return <>{children}</>;
}
